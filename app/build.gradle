plugins {
    id 'com.android.application'
}

android {
    namespace 'com.cessna140.flighttest'
    compileSdk 35

    defaultConfig {
        applicationId 'com.cessna140.flighttest'
        minSdk 23
        targetSdk 35
        versionCode 3
        versionName '1.2'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
